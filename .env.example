# Aika DBA 環境變數設定範例
# 複製此文件為 .env 並填入你的 API 金鑰

# OpenAI API 設定 (當 llm.provider = "openai" 時需要)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# LLM 模型設定
LLM_MODEL=gpt-4
# LLM_MODEL=gpt-3.5-turbo  # 或者使用 GPT-3.5

# 本地 LLM 設定 (當 llm.provider = "local" 或 "ollama" 時需要)
LLM_HOST=localhost
LLM_PORT=8080

# LLM 參數設定
LLM_CONTEXT_SIZE=4096
LLM_MAX_TOKENS=1024

# 資料庫設定 (如果需要覆蓋 config.yaml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=ecommerce_user
DB_PASSWORD=ecommerce_pass
DB_NAME=ecommerce_db